{{ .Chart.Name }} has been deployed.
{{- if and .Values.networkPolicy.enabled (not .Values.networkPolicy.egress.servicexBackend.namespaceSelector.matchLabels) (not .Values.networkPolicy.egress.servicexBackend.podSelector.matchLabels) }}

##############################################################################
WARNING: networkPolicy.egress.servicexBackend.namespaceSelector/podSelector
are both still empty — this NetworkPolicy's egress rule for port
{{ .Values.networkPolicy.egress.servicexBackend.port }} selects nothing, so
the redeem call to your ServiceX backend ({{ .Values.config.servicexBackendUrl }})
will be blocked. Set both selectors to match your ServiceX deployment's
namespace and pods.
##############################################################################
{{- end }}
