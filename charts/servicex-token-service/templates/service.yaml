apiVersion: v1
kind: Service
metadata:
  name: {{ include "servicex-token-service.fullname" . }}
  labels:
    {{- include "servicex-token-service.labels" . | nindent 4 }}
spec:
  # ClusterIP only: the sole intended client is the in-cluster broker, and
  # the NetworkPolicy enforces exactly that.
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servicex-token-service.selectorLabels" . | nindent 4 }}
